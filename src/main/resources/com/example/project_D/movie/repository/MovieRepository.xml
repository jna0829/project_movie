<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.project_D.movie.repository.MovieRepository">

    <select id="listAll" resultType="com.example.project_D.movie.entity.Movie">
        SELECT movieCd, movieNm, directors, actors, showTm, openDt
        FROM movie
    </select>

    <select id="findOne" resultType="com.example.project_D.movie.entity.Movie">
        SELECT movieCd, movieNm, directors, actors, showTm, openDt
        FROM movie
        WHERE movieCd=#{movieCd}
    </select>

    <insert id="add">
        INSERT INTO movie
            (movieCd, movieNm, directors, actors, showTm, openDt)
        VALUES
            (#{movieCd}, #{movieNm}, #{directors}, #{actors}, #{showTm}, #{openDt})
    </insert>

</mapper>



